# Public-tree allowlist: the checked-in manifest of
# what ships in the kvit-notes public repository. Omission by default,
# inclusion by decision - a path absent here does not export, whatever it
# is. Lines are git pathspecs consumed by tools/export-public-tree.sh;
# :(exclude) entries carve private code out of included trees.
#
# Deliberately absent, kept in the private planning tree: the product and
# milestone planning documents, the premium agent module (src/agent,
# qml/agent, tests/agent, agent_resources.qrc), and the tracked perf log
# with machine-local paths (tests/perf/live).
CMakeLists.txt
CMakePresets.json
build.sh
resources.qrc
.gitignore
.gitattributes
LICENSE
README.md
CHANGELOG.md
CONTRIBUTING.md
CODE_OF_CONDUCT.md
SECURITY.md
THIRD-PARTY-NOTICES.md
usage.md
features.md
devel.md
.github
docs
packaging
qml
src
tests
third_party
tools
screenshots/press
:(exclude)src/agent
:(exclude)qml/agent
:(exclude)tests/agent
:(exclude)tests/perf/live
# The machinery that produces and checks the public tree stays in the
# private tree. It is of no use to a contributor working in the public
# repository, it names private paths, and its denylist necessarily
# enumerates the private planning documents it exists to keep out.
:(exclude)tools/export-public-tree.sh
:(exclude)tools/export-public-update.sh
:(exclude)tools/export-gates.sh
:(exclude)tools/check-md-links.py
